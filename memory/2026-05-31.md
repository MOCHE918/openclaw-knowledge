# 2026-05-31 记忆

## 今日主要活动

### Python 环境确认
- Python 3.14.5 + pip 26.1.1 确认可用
- 安装路径：`E:\python相关软件\`
- PATH 已配置，刷新后正常

### 课件制作 — 四年级语文上册第一单元《自然之美》

**方案决策：**
- 放弃 PPTist/Focusky，选择 HTML5 课件 + 视频讲解方案
- 设计风格：教育科技（蓝紫+暖黄色调），适合小学生
- 所有汉字使用 `<ruby><rt>` 标签标注拼音，浏览器原生渲染
- 拼音 CSS：`rt { font-size: 0.65em; color: #8B8B8B; }`

**交付物：**
| 文件 | 位置 | 说明 |
|------|------|------|
| `chinese_unit1.html` | workspace | 原始全拼音课件（44KB） |
| `chinese_unit1_video.mp4` | workspace | 最终视频（2.63MB, 105.9s, 1920×1080, 10fps） |
| `02.html` | 桌面 | 用户要求的最新版本（修复响应式+语音队列+BGM路径等） |
| `index.html` 及 8 页独立 HTML | `D:\墨澈的教材\四年级语文上册第一单元\` | 独立分页课件 |
| 各页截图 + MP3 配音 + 中间文件 | `C:\Users\ADMINI~1\AppData\Local\Temp\courseware_cn\` | 构建中间产物 |

**视频制作流水线：**
1. 创建独立单页 HTML → Edge `--headless --screenshot` 截图（1920×1080）
2. Edge TTS 生成 8 个 MP3 配音
3. 生成 ASS 字幕（微软雅黑 28 号）
4. FFmpeg `subtitles=` 滤镜烧录硬字幕 + `concat` 合成
5. BGM 未下载（Mixkit/Pixabay 全部 403/404），跳过

**02.html 改进（vs chinese_unit1.html）：**
- BGM 文件改为 `bgm.mp3`（更通用）
- 拼音 `rt` 字号 0.65em（原 0.55em）
- 新增 `@media (max-width: 800px)` 响应式适配
- 语音加载队列（`voicesLoaded` / `pendingSpeak`）：等待 SpeechSynthesis 就绪后再朗读
- P7 测验题、P8 结语部分去除过密 ruby 标注，阅读更清爽
- `setInterval` 轮询语音库加载状态

### 用户偏好
- 注重拼音标注的视觉呈现（ruby 标签 > ASS 字幕层）
- 课件面向小学生，需要兼顾可读性和美观
- 期望产出可售卖的参赛级课件视频
- 电脑桌面实际路径：`C:\Users\Administrator\Desktop\`（非便携环境映射）

### 技术要点
- FFmpeg 字幕滤镜：`subtitles='C\:/_build/subs.ass'`（冒号需转义 `\:`）
- `ass=` 滤镜解析路径有歧义，优先用 `subtitles=`
- Edge 无头截图：需独立单页 HTML，`scrollIntoView` 在无头模式下不可靠
- DeepSeek API 余额：¥9.75

### 08:33 PM �� �칫����
- **��ֹ�ѹ����м��ļ��� C ��**����ͼ����������Ļ��FFmpeg �ֶεȣ���Ӱ��칫Ч��
- ��������Ŀ¼ͳһ������� D �̣��� D:\courseware_build\��%USERPROFILE%\Desktop\build\ �ȣ�
- ���ս������ D:\ī���Ľ̲�\ ������
