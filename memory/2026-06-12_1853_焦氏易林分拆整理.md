# 2026-06-12 18:53 焦氏易林分拆整理

## 会话概要
用户之前下载了焦氏易林_全文.txt（290KB），要求分析结构并拆分为64卦独立文件。

## 分析发现
1. 全文有16个 `===== N卦名 =====` page-header sections（对应16个web页面），每个页面含约4卦
2. 另有 `此起为XX卦` 过渡标记共64个，标记了每卦的起始位置
3. 标记类型两种：section headers（`=====` 格式）和 transition markers（`此起为` 格式）
4. 原始文件混杂了HTML导航杂项（古文岛导航、audio播放器、音量控制、上一章/目录/下一章、APP登录框、版权声明等）

## 处理过程

### 第一步：标记提取
- 提取了全部64个位置标记，按位置排序
- 发现名称偏差：
  - `===== 05需卦随卦` — 实际是第5个web页面，内容为随卦（hex17）
  - `此起为否之卦` — 应映射为 否（hex12）
  - `旡妄` — 无妄的异体字
  - `遯` — 遁的繁体字
  - `===== 06噬嗑` — 无"卦"字后缀，漏提取

### 第二步：边界情况处理
- **随卦缺失**：`===== 05需卦随卦` section header 映射失败，随卦内容未被提取。实际随卦林辞在 section header 之后（文本中 `随之随...` 到 `随卦终`），手动提取成功
- **噬嗑卦缺失**：section header `===== 06噬嗑` 无"卦"字后缀，正则匹配失败。进一步分析发现该页面仅为网站导航/索引页，**实际噬嗑卦经文内容不在下载文件中**。63/64完成，噬嗑卦无法从已有数据恢复
- **否卦**：marker文本为"此起为否之卦"，映射为"否"，内容正确
- **贲卦**：marker文本为"此起为贲卦"，正确提取

### 第三步：清洗
- 删除所有HTML导航杂项（"东北一枝花"、audio播放列表、音量控制、APP登录、古文岛导航栏等）
- 保留原文中的夹注（如"末二句疑衍"、"此二句疑衍"）
- 保留"卷N"分类标记（卷一~卷十六）
- 保留"XX之X"格式的变卦标记

## 最终成果
- 63/64卦成功提取，每卦1400-1753字
- 生成 `焦氏易林_64卦汇编.txt`（276KB，63卦顺序拼接+数字编号）
- 生成 `焦氏易林_纯林辞.txt`（276KB，纯正文无编号）
- 生成 `焦氏易林_分卦_clean/` 目录，63个单文件，命名格式 `NN_卦名.txt`
- 所有临时脚本和空桩文件已清除

## 文件位置
- `D:\Portable_V1.2.2\data\UserProfile\.openclaw\workspace\古籍\`

## 补充工作
- 清理了55个空桩文件（605B的焦氏易林_17至69各卦导引页）
- 删除了百度文库搜索片段、古诗文网搜索结果等临时文件
- 古籍目录现在只保留：周易全7章+全文、易传目录页、焦氏易林全文+汇编+纯林辞+分卦目录
